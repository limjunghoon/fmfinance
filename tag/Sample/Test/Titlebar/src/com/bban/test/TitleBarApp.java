package com.bban.test;

import android.os.Bundle;

public class TitleBarApp extends BaseApp {
    /** Called when the activity is first created. */
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        
        setContentView(R.layout.main, true);
        
 //       setTitleButtonListener();
 //       setContentView(R.layout.main);
 //       LinearLayout linear = new LinearLayout(this);
//        linear.setOrientation(LinearLayout.VERTICAL);
//        LinearLayout titleLinear = (LinearLayout)View.inflate(this, R.layout.titlebar, null);
        
//        linear.addView(titleLinear);
//        linear.addView(mainLinear);
//        setContentView(linear);
    }
}